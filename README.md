== micro editor binaries, text filter/tool

append snippet into .profile or .bashrc/.zshrc

```bash
# cut start ----

export EDITOR=micro
export PATH="$HOME/.config/micro/bin/:$PATH"

# cut end ----
```

restart/reboot  
or  
source .profile|.bashrc|.zshrc
